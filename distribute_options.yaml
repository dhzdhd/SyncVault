output: dist/
releases:
  - name: linux
    jobs:
      - name: release-linux-rpm
        package:
          platform: linux
          target: rpm
      # - name: release-linux-deb
      #   package:
      #     platform: linux
      #     target: deb
